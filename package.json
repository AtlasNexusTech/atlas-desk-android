{
  "name": "atlas-desk-android",
  "version": "0.1.0",
  "description": "Atlas Desk — P2P Remote Desktop Android Client",
  "scripts": {
    "sync": "npx cap sync",
    "android": "npx cap open android",
    "build": "npx cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/cli": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  }
}
